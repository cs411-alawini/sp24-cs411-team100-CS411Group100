import { useTranslation } from "react-i18next";
import { AppSideBarProps } from "./AppSidebar.constants";

type Props = AppSideBarProps;

const AppSideBar = (props: Props) => {
  const { t } = useTranslation();

  return (
    <>
    </>
  );
};
export default AppSideBar;
